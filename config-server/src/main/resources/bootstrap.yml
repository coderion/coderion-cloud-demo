server:
  port: 8888
spring:
  application:
    name: pl.coderion.common.config-server
  profiles:
    active: native # because the pl.coderion.common.config server uses a git repo by default
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/pl.coderion.common.config
#        git:
#          uri: https://github.com/coderion/coderion-cloud-demo-config.git
