server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: native # because the config server uses a git repo by default
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/pl.coderion.common.config
#        git:
#          uri: https://github.com/coderion/coderion-cloud-demo-config.git